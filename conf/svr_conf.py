CONFIG_DICT = {
    # server
    'svr.name': 'server_name',
    'svr.force_close_delay': 10,

    # log
    'log.level': 'INFO',
    'log.format': '<%(levelname)s: %s(%(process)d)> [%(filename)s: %(lineno)d] >> %(message)s',

    # other
    'other.x': 1000
}
