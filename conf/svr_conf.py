CONFIG_DICT = {
    # server
    'svr.name': 'my_server',
    'svr.force_close_delay': 10,

    # log
    'log.level': 'INFO',
    'log.format': '< %(levelname)s: %(name)s(%(process)d) > [%(filename)s: %(lineno)d] >> %(message)s ',

    # other
    'other.x': 5
}
